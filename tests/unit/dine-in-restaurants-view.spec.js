describe("Dine-inRestaurants.vue", () => {
  //
  it('renders search inputs for "Search by name" and "Search by cuisine"', () => {});

  it('emits "search" event when "Search" button is clicked', async () => {});

  it("displays an error message for wrong seach inputs", () => {});

  //
  it("renders RestaurantDetailCard for each Dine-in Restaurant", () => {});

  it('shows filtered RestaurantDetailCards for each Dine-in Restaurant accdording to "Search by name" and "Search by cuisine', () => {});

  it("renders RestaurantDetailCards for filtered by Restaurants in Dine-in", () => {});

  it('displays "View More" button', () => {});

  it('emits "load-more" event when "View More" button is clicked', async () => {});
});
